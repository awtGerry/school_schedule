/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    './**/*.{html,js,ts,svelte}',
  ],
  theme: {
    /* Importar colores que se utilizaran en el proyecto */
    colors: {
    },

    /* Importar fuentes por defecto que se utilizaran en el proyecto */
    fontFamily: {
    },

    extend: {},
  },
  plugins: [],
}

